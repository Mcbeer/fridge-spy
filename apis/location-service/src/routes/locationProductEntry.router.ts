import { Router } from "express";

const router = Router();

// Add X amount of entries to a location product

// Remove X amount of entries from a location product

export { router as locationProductEntryRouter };
